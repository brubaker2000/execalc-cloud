# TRUE NORTH — Execalc

## Category Statement
Execalc is a **governed, normative, and informed** executive cognition system. It delivers boardroom-grade synthesis and judgment by operating **one layer above** models, tools, agents, and data.

Execalc treats the LLM as a **language substrate** — not the system.

### The Three-Layer Superstrate

These three words are not synonyms. They describe distinct and non-overlapping layers:

**Governed** — the regulative layer. Execalc enforces safety, compliance, and tenant controls. It will not cross defined ethical and operational limits. This is what every responsible AI deployment has. It is necessary but not sufficient.

**Normative** — the constitutive layer. The Prime Directive, the three lenses, the Core 7 frameworks, the GAQP standards: these are not restrictions on what the system does. They are what the system is. The system does not merely avoid bad reasoning — it is architecturally built to pursue value and clarity according to explicit doctrine. A system that has only governance can be stopped from doing harm. A normative system is shaped to do good. This is the distinction that makes Execalc a different category from a guardrailed chatbot.

**Informed** — the knowledge layer. The EKE corpus, the thought leadership bench, the heuristic library, the organizational memory: these are not retrieved when the operator asks for them. They activate when the situation calls for them. The system carries encoded judgment and retrieves it on the basis of pattern recognition, not keyword search.

Most AI systems are governed. Few are normative. None are informed in this sense.

The competitive moat is not the model. It is the normative architecture and the informed corpus that the model operates within.

---

## What Execalc Is
Execalc is:

- A **judgment engine** governed by explicit doctrine (Prime Directive + invariants)
- A **normative system** — constituted to pursue value and clarity, not merely constrained from harm
- A **cognition spine** that converts unstructured input into structured executive clarity
- A **scenario-triggered runtime** (situations first; pathways follow)
- A **memory-disciplined system** with admission rules and dormant classes
- A **traceable system**: outputs are attributable, auditable, and replayable
- A **sovereign governor** of execution: if workers exist, they are subordinated

---

## What Execalc Is Not
Execalc is not:

- An ungoverned chatbot
- An agent swarm that self-directs actions
- A workflow automation product as its core identity
- A dashboard-only “insights” tool
- A system that relies on chat history as its source of truth

---

## Sovereignty Model
The system is defined by separation of powers:

- **Integrations provide access**
- **Workers perform labor**
- **Informants supply signal**
- **Execalc alone produces judgment**

Workers may draft and execute bounded tasks only when authorized by Execalc under policy gates and proof standards.

---

## Governance Spine
Execalc governance is enforced by:
- Prime Directive doctrine — delivers value (monetization) or clarity (operational); judged through three lenses: Assets/Liabilities, Risk/Reward, Supply/Demand; all three must be evaluated; anything that does not pass these gates does not get acted on
- Canonized invariants (non-negotiables enforced by tests)
- Proof-gated state transitions for irreversible actions
- Tenant isolation and auditability by default

If doctrine and chat conflict, doctrine wins.

If doctrine and code conflict, invariants and tests must be updated deliberately and versioned.

---

## Runtime Activation Hierarchy
Execalc operates by a governed cascade:

1. **Situation detection** (Executive Scenario triggers first)
2. **Activation pathway selection**
3. Optional invocation of:
   - Executive protocols (carats)
   - Thinker-attributed logic units (attribution downstream)
4. **Prime Directive filter**
5. **Structured executive output** (traceability metadata included)

---

## Memory Doctrine
Execalc maintains controlled memory:

- **Active memory** may trigger governed reasoning when recalled
- **Dormant memory** is recallable but does not trigger reflexes or execution until promoted
- All memory admissions require metadata: source, domain, confidence, role-weighting (when applicable), timestamps

---

## Build True North
The repository is the constitutional source of truth:

- Chat is for ideation
- Repo is doctrine
- Code is enforcement

Anything not canonized in the repo is not considered real.

---

## Stage 6 Definition
Stage 6 is complete when Execalc is:

1. Multi-tenant safe by default
2. Governed by invariant enforcement
3. Extensible without mutation
4. Auditable and replayable
5. Execution-subordinate
6. Operationally shippable

Stage 6 is structurally complete, not feature complete.

---

## Required Reading for Any New Dev Chat
A new dev chat must begin by reading:

- `docs/vision/TRUE_NORTH.md`
- `docs/vision/STAGE_MAP.md`
- `docs/EXECALC_INVARIANTS.md`
- `docs/vision/CANONIZATION_PROTOCOL.md`

The next move is determined from these documents, not from prior chat history.
